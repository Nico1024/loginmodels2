@page "/"
@using Loginnmodels2.Data
@inject IModalService Modal

<h1>Hello, world!</h1>

Welcome to your new app.

<button @onclick="@(() => Modal.Show<Counter>("My Movies"))" class="btn btn-primary">View Movies</button>




@*<button @onclick="@(() => Modal.Show<buildertest>("asdasd", parameters))" class="btn btn-primary">View Movies</button>*@
<button @onclick="LaunchWindow" class="btn btn-primary">Launch Window</button>

<Frm_NewObject4 Context="dx" />

@code
{
    private DataX dx = new DataX();
    private UserHasCases a = new UserHasCases();
    ModalParameters parameters = new ModalParameters();

    Dictionary<string, string> FieldIdentifiers { get; set; } = new Dictionary<string, string> { { "Name", "string" }, { "Phone", "string" } };

    private void LaunchWindow()
    {
        parameters.Add("Generic", a);
        //Modal.Show<Counter>("asdasd", parameters);
        //Modal.Show<Frm_NewObject1>("asdasd", parameters);
    }


}